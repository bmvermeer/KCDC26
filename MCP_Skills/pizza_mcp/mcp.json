{
  "mcpServers": {
    "Pizza MCP Server": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "mcp", "run", "pizza_concierge_mcp.py"]
    },
    "Delivery Tracker MCP Server": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "mcp", "run", "delivery_tracker_mcp.py"]
    }
  }
}
